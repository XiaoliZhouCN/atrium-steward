# 分支管理
**文档说明**
- 本文档一站式整理 Harvey 工作区所有git项目的分支情况
- 文档管理者：AtriumPytools

# AtriumSteward
- main
    默认分支；当前为空分支，仅保留 `README.md`。
- develop
    集成开发分支；当前已合并 `dev/basic`，包含 AtriumSteward 的现有工作区代码与文档。
- dev/basic
    基础开发分支；当前承载 AtriumSteward 的最新开发内容。

# AtriumPytools
- main
    默认分支；当前为空分支，仅保留 `README.md`。
- develop
    集成基线分支；当前与 `main` 一致，作为工具分支的上游基线。
- dev/basic
    工具公共基线分支；当前与 `main` 一致，预留共享骨架与公共能力。
- dev/basic-colorpicker
    `colorpicker` 工具分支；基于 `dev/basic`，当前包含取色工具的现有实现。

# AtriumNote
- main
    默认分支；当前为空分支，仅保留 `README.md`。
- develop
    集成基线分支；当前与 `main` 一致，作为 AtriumNote 内容分支的上游基线。
- dev/basic
    基础开发分支；当前包含 AtriumNote 的现有目录结构、日志模板与资料内容。

# NexusRenderer
- main
    默认分支；当前为空分支，仅保留 `README.md`。
- develop
    集成基线分支；当前与 `main` 一致，预留后续开发汇总入口。
- dev/basic
    基础开发分支；当前与 `main` 一致，等待实际开发内容落地。

# ChromaCMS
- main
    默认分支；当前为空分支，仅保留 `README.md`。
- develop
    集成基线分支；当前与 `main` 一致，预留后续开发汇总入口。
- dev/basic
    基础开发分支；当前与 `main` 一致，等待实际开发内容落地。
